---
applications:
- name: service1
  memory: 512M
  instances: 1
  host: service1-${random-word}
  domain: cfapps.io
  path: target/service1.jar
  services:
      - server
  env:
    SPRING_PROFILES_ACTIVE: cloud
    DEBUG: "true"
    debug: "true"
